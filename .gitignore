# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# uv lockfile — this is a library, don't pin the lockfile
uv.lock

# Personal souls — never commit someone's memory
*.fafm
!examples/*.fafm

# FAF derived artifacts
*.fafb
project.html
